[project]
name = "hearth-infra"
version = "0.1.0"
description = "Ansible automations for setting up and maintaining the homelab."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible>=12.2.0",
]
